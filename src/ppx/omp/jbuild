(jbuild_version 1)

(library
 ((name ppx_await)
  (public_name ppx_await)
  (synopsis "async-await syntax for repromise (actually, just -await)")
  (kind ppx_rewriter)
  (ppx_runtime_libraries (repromise))
  (flags (:standard -w +A-4))
  (libraries (ocaml-migrate-parsetree))))
